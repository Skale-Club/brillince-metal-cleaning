services:
  - type: web
    name: brillince-metal-cleaning
    runtime: node
    region: virginia
    plan: free
    buildCommand: npm install --include=dev && npm run build
    startCommand: npm run start
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production
